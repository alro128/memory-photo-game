//
//  Board.swift
//  MemoryPhotoGame
//
//  Created by taiger on 26/06/2021.
//

import SwiftUI

struct Board {
    
    @State var cards : [Card]
/*
    init() {
        
    }
    
    init(images:[Image]) {
        for i in 0...2 {
            cards.append(Card(id: i, faceUpImage: images[i]))
            print("Card added with id \(i) ")
            print("Card added with id \(i+1) ")
        }
    }
    
    func replaceCardImage(i: Int, image:Image) {
        switch i {
        case 0:
            self.cards[i] = Card(id: i, faceUpImage: image)
            self.cards[i+3] = Card(id: i+3, faceUpImage: image)
        case 1:
            self.cards[i] = Card(id: i, faceUpImage: image)
            self.cards[i+3] = Card(id: i+3, faceUpImage: image)
        case 2:
            self.cards[i] = Card(id: i, faceUpImage: image)
            self.cards[i+3] = Card(id: i+3, faceUpImage: image)
        default:
            print("no card index match")
        }
        
    }
    
    func shuffle() {
        
    }
    
    func countFlipCards() {
        
    }
    
    func startGame() {
        
    }
    */
}
